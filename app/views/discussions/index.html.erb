<% provide(:title, 'All discussions') %>
<% content_for :left do %>
  <div class="index-header">
    <h2>All Discussions</h2>
    <div class="pull-right"><%= render 'btn_create' %></div>
  </div>
  <div class="section-container">
    <div class="tabbable">
      <%= render 'tabs' %>
    </div>
    <div class="tab-content">
      <%= render 'tab_content' %>
    </div>
  </div>
<% end %>

<% content_for :right do %>
  place_holder
<% end %>