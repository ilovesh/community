    <div class="caption">
      <h3><%= link_to truncate(list.title, length: 80), list %></h3>
      <div class="pull-right list-info">
        <p class="label label-inverse"><%= pluralize(list.courses.count, "course") %></p>
        <p>by <%= link_to list.user.username, list.user %></p>
      </div>
    </div>