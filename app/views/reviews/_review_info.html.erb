<% if current_page? review.course %>
  <%= render partial: 'shared/small_gravatar', locals: { post: review } %>
<% else %>
  <%= render partial: 'shared/large_gravatar', locals: { post: review } %>
<% end %>
<%= link_to review.user.username, review.user %>
<%= render partial: 'shared/timestamp', locals: { post: review } %>
<% if current_user?(review.user) %>
  <%= link_to "edit",   edit_review_path(review) %>
  <%= link_to "delete", review,
              method: :delete,
              data: { confirm: "Are you sure to delete this review?" },
              title: review.body %>
<% end %>