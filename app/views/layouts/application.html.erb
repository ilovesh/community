<!DOCTYPE html>
<html>
  <head>
    <link href='http://fonts.googleapis.com/css?family=Crete+Round' rel='stylesheet' type='text/css'>
    <link href='http://fonts.googleapis.com/css?family=Monda|Merriweather|Montserrat|Nunito' rel='stylesheet' type='text/css'>
    <title><%= full_title(yield(:title)) %></title>
    <%= favicon_link_tag %>
	  <%= include_gon %>
    <%= stylesheet_link_tag    "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= render 'layouts/shim' %>
    <script type="text/javascript">

      var _gaq = _gaq || [];
      _gaq.push(['_setAccount', 'UA-37572437-1']);
      _gaq.push(['_setDomainName', 'courigin.com']);
      _gaq.push(['_trackPageview']);

      (function() {
        var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
        ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
        var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
      })();

    </script>
  </head>
  <body class="<%= body_bg(yield(:background)) %>">
    <%= render 'layouts/header' %>
    <div class="container-fluid">
      
      <div class="row-fluid">
        <% flash.each do |key, value| %>
          <div class="alert alert-<%= key %>"><%= value %></div>      
        <% end %>
        <%= yield %>
        <div class="left-column span7 offset1">
          <%= yield :left %>
        </div>      
        <div class="right-column span3">
          <%= yield :right %>
        </div>
      <%= debug(params) if Rails.env.development? %>
      </div>
    </div>
    <%= render 'layouts/footer' %>
  </body>
</html>